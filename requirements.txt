# SPAMURAI Contact Allocator Dependencies

# Core data processing
pandas>=1.5.0
openpyxl>=3.0.0

# Google Sheets API (read-only, no credentials)
requests>=2.28.0

# Optional: For write operations (not used by allocator)
# gspread>=5.0.0
# google-auth>=2.0.0
